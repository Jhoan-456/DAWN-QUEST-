extends Node

var fps_visibles : bool = true
var version_juego : String = "v0.0.2 - alpha "
var version_visible : bool = true

# Guardaremos la ruta del personaje seleccionado
var ruta_personaje_seleccionado: String = "res://escenas/characters/personajes/FINNES/finnes.tscn"

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
